# DnsZone

Represents DNS zones.

**Labels:** `:ArmResource:DnsZone`

**Properties:**

- `id` - DNS zone resource ID (primary key)
- `nameServers` - Array of name servers
- `numberOfRecordSets` - Number of record sets
- `zoneType` - Zone type
