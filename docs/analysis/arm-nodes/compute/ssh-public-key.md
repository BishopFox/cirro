# SSHPublicKey

Represents SSH public keys for Linux VMs.

**Labels:** `:ArmResource:SSHPublicKey`

**Properties:**

- `id` - SSH key resource ID (primary key)
- `publicKey` - The SSH public key content
