# VisualStudioAccount

Represents Azure DevOps (Visual Studio) accounts.

**Labels:** `:ArmResource:VisualStudioAccount`

**Properties:**

- `id` - Visual Studio account resource ID (primary key)
- `accountURL` - Account URL for the Visual Studio/Azure DevOps organization