# GraphObject

<h2>GraphObject (Base Label)</h2>

All Microsoft Graph entities inherit from the base `GraphObject` label and include the `id` property.

**Common Properties:**

- `id` - Unique identifier (primary key)