# Certificate

Represents certificates for applications and service principals.

**Labels:** `:Certificate`

**Properties:**

- `thumbprint` - Certificate thumbprint (primary key)
- `displayName` - Certificate display name
- `startDateTime` - Start date/time
- `endDateTime` - End date/time
- `type` - Certificate type
- `usage` - Certificate usage
